<!DOCTYPE html>
<html lang="kr">
	<head>
		<meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0,  maximum-scale=1.0, shrink-to-fit=no" />
		<meta name="format-detection" content="telephone=no" />
		<link rel="icon" href="/favicon/favicon.ico" type="image/x-icon">
		<title><%= item.name %> - BDR - 검은사막 레시피</title>

		<link rel="stylesheet" href="/css/main.css" type="text/css" />
		<script type="text/javascript" src="/lib/lodash.4.17.15.js"></script>
		<script type="text/javascript" src="/js/main.js"></script>
	</head>
	<body>
		<div class="container">
			<div class="searchWrap"><input type="text" id="searchText" placeholder="검색할 아이템명을 입력하세요." spellcheck="false"></div>
			<div class="content">
				<div class="itemImg"><img src="<%= item.itemImage.imgUrl %>" /></div>
				<div class="itemName"><%= item.name %></div>
				<div class="itemDesc"><%- item.desc.replace('\n', '<br />') %></div>
				<hr class="separator">
				<div class="subtitle">획득 방법</div>
				<div class="earnList">
					<% for (var i = 0 ; Array.isArray(earn) && i < earn.length; i++) {%>
					<div class="row">
						<!-- <div><%= earn[i].type == 'craft' ? '제작' : '습득' %></div> -->
						<div><%= earn[i].work %></div>
						<div><%= earn[i].path %></div>
						<% for (var x = 0 ; Array.isArray(earn[i].craftList) && x < earn[i].craftList.length; x++) {%>
							<div><a href="<%= earn[i].craftList[x].url %>"><%= earn[i].craftList[x].itemId %></a></div>
							<div><%= earn[i].craftList[x].name %></div>
							<div><%= earn[i].craftList[x].count %><input class=count type=text value=""></div>
						<% } %>
					</div>
					<% } %>
				</div>
				<hr class="separator">
				<div class="subtitle"><%= item.name %> - 제작가능 아이템</div>
				<% for (var y = 0 ; Array.isArray(usages) && y < usages.length; y++) {%>
				<div><%= usages[y].resultItemId %></div>
				<div class="resultItemName"><a href="<%= usages[y].url %>"><%= usages[y].resultItemName %></a></div>
				<% } %>
			</div>
		</div>
	</body>
</html>
